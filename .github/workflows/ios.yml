name: NeuroGuia iOS Release

on:
  push:
    branches: [ "main" ]

jobs:
  deploy-ios:
    name: Build e Deploy iOS App Store
    runs-on: macos-latest

    env:
      APP_STORE_CONNECT_KEY: ${{ secrets.APP_STORE_CONNECT_KEY }}
      APP_STORE_KEY_ID: ${{ secrets.APP_STORE_KEY_ID }}
      APP_STORE_ISSUER_ID: ${{ secrets.APP_STORE_ISSUER_ID }}

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar o Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Configurar o Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Instalar Dependências do Flutter
        run: |
          flutter clean
          flutter pub get

      - name: Criar Chaveiro e Gerar Certificado na Nuvem
        run: |
          # 1. Instala pulando a trava de ambiente gerenciado externo
          pip3 install codemagic-cli-tools --break-system-packages
          
          # 2. Adiciona o caminho dos binários do Python ao PATH do GitHub Actions
          echo "$(python3 -m site --user-base)/bin" >> $GITHUB_PATH
          export PATH="$(python3 -m site --user-base)/bin:$PATH"
          
          # 3. Inicializa o chaveiro seguro na máquina virtual
          keychain initialize
          
          # 4. Cria e busca o certificado de distribuição na Apple
          app-store-connect fetch-signing-files com.maoamiga.app.app \
            --issuer-id="$APP_STORE_ISSUER_ID" \
            --key-id="$APP_STORE_KEY_ID" \
            --private-key="$APP_STORE_CONNECT_KEY" \
            --create \
            --type=IOS_APP_STORE \
            --platform=IOS
          
          # 5. Vincula o certificado ao chaveiro local do Mac
          keychain add-certificates

      - name: Compilar o pacote IPA (Release)
        run: |
          flutter build ipa --release --export-options-plist=ios/export_options.plist

      - name: Enviar o App para o TestFlight
        run: |
          xcrun altool --upload-app --type ios \
            -f build/ios/ipa/*.ipa \
            --apiKey "$APP_STORE_KEY_ID" \
            --apiIssuer "$APP_STORE_ISSUER_ID"